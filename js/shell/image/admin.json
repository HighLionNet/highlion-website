{
  "schema": 1,
  "notice": "root is an emergency session console for the simulated lab only. It cannot reach the host.",
  "root": {
    "enabled": true,
    "authentication": "server-backed-password"
  },
  "packPolicy": {
    "manifest": "packs/manifest.json",
    "maxPacks": 64,
    "allowExecutableScripts": true,
    "allowArbitraryNetworkHosts": false
  }
}
